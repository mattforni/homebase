---
paths:
  - "**/mcp/**"
  - "**/*mcp*.{rb,ts,js,py}"
---

# MCP Server Method

Building or changing an MCP server follows `~/Eudaimonia/Craft/Development/Method/MCP/`: `design.md` for tool names, verbs, nouns, edits, annotations, and errors; `auth.md` for lazy auth and OAuth. Read the relevant entry before adding or renaming a tool or touching auth.
